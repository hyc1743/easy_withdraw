
## 多搬砖任务并行执行

- 在搬砖页填写一组配置，点击“添加并启动任务”，修改配置后再次添加。
- 分别添加 CEX 到 DEX、DEX 到 CEX 任务，确认列表同时显示运行中。
- 点击不同任务，确认详情、日志和进度对应所选任务，刷新页面后仍能看到全部运行任务。
- 停止其中一个任务，确认其余任务继续刷新和执行；点击“继续”恢复该任务。
- 在操作尚未结束时停止任务，确认当前操作结束后不会再次调度；删除任务后不会重新出现。
- 服务重启后，有下次执行时间的任务恢复调度；执行中断且结果不确定的任务保持停止，避免自动重复执行。
- 非搬砖任务仍维持原有互斥规则。停止不会撤回已经提交的提现或链上交易。
- 自动回归（使用隔离数据目录）：
  `EW_DATA_DIR=$(mktemp -d) node --import tsx --test tests/tasks-multiple-arbitrage.test.ts tests/tasks-runtime.test.ts tests/tasks-hydration-interrupted.test.ts`

## 多任务监控界面

- 桌面端：确认列表与详情双栏展示；选择第二个任务，等待自动刷新后仍显示第二个任务，选中行保持高亮。
- 用币种、链、编号搜索，切换状态筛选；统计保持总览数量，无匹配结果显示明确空状态。
- “添加任务”打开配置抽屉，关闭或按 Escape 保留未提交配置；启动成功后自动关闭，失败时保持表单并显示错误。
- 搬砖详情显示阶段、余额、阈值和累计次数，不再显示无限循环任务的百分比进度条。
- 手机端：点击任务进入全屏详情，“返回任务列表”回到列表；确认没有横向溢出。
- 停止、继续、删除分别作用于指定任务；删除其他任务不清空当前详情，取消删除不改变数据。
- 浏览器验证截图（模拟任务数据，不涉及真实交易）：`docs/screenshots/tasks-desktop.png`、`tasks-mobile-list.png`、`tasks-mobile-detail.png`。

## 搬砖来源与目标标注

- 列表和运行面板均显示“来源 → 目标”；交易所来源带交易所名称，链上账户显示钱包名称。
- DEX 到 CEX 的目标按充值地址匹配地址簿备注；未匹配时显示“未标注地址”和实际地址，不猜测归属账户。
- 叠加跨链的 CEX 到 DEX 任务显示最终收款钱包，并单列途经提现地址，避免与中转钱包混淆。
- 搜索支持两端账户名称、地址及交易所名称；详情展示完整地址，列表缩略展示。
