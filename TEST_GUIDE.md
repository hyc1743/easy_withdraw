# Easy Withdraw 测试指引

本指引用于验证以下目标是否生效：

- 仅本机访问（默认 localhost）
- 定时提现进度可同步
- 关闭浏览器不影响提现操作
- 会话锁定后，已启动定时任务继续执行
- SQLite 持久化与服务重启恢复
- `unlock` 防暴力破解限流

## 1. 测试前准备

### 1.1 环境要求

- Node.js 18+（建议 20+）
- npm 可访问外网
- 可用的 Gate 测试账号或可容错的测试参数

### 1.2 安装依赖

```bash
npm install
```

### 1.3 启动服务

开发模式：

```bash
npm run dev
```

生产模式：

```bash
npm start
```

默认访问地址：

- 本机：`http://127.0.0.1:4217`

## 2. SQLite 初始化与迁移验证

### 步骤

1. 启动服务后检查目录 `~/.easy_withdraw/`
2. 确认存在 `easy_withdraw.db`
3. 若存在历史 `config.json/history.json`，登录后检查账户、模板、历史是否可见

### 预期结果

- SQLite 数据库自动创建
- 旧 JSON 数据自动迁移并可读取

## 3. 单次提现持久化验证

### 步骤

1. 解锁系统
2. 添加地址簿条目（任意测试地址）
3. 执行一次单次提现（成功/失败均记录）
4. 重启服务后再次查看地址簿与历史页

### 预期结果

- 地址簿数据持久化
- 历史记录持久化

## 4. 定时任务同步与浏览器关闭验证

### 步骤

1. 解锁后启动定时提现（建议：间隔 `15s`，次数 `6`）
2. 观察执行 1 次后，直接关闭浏览器
3. 等待约 1 分钟，再次打开页面并解锁
4. 进入“提现”页查看进度面板
5. 刷新页面（F5）再次观察

### 预期结果

- 任务持续执行，不因浏览器关闭中断
- 页面重新打开后可同步到最新进度
- 刷新页面不重置进度

## 5. 锁定后继续执行验证（关键）

### 步骤

1. 启动定时提现（建议：间隔 `20s`，次数 `5`）
2. 立即点击“锁定”
3. 等待 40~60 秒
4. 重新解锁并查看进度

### 预期结果

- 锁定期间定时任务继续推进
- 进度与日志显示锁定期间产生的执行记录

## 6. 服务重启恢复验证

### 步骤

1. 启动定时提现（建议：间隔 `20s`，次数 `8`）
2. 任务执行 1~2 次后，停止服务（`Ctrl+C`）
3. 重新启动服务并解锁
4. 打开“提现”页查看进度

### 预期结果

- 运行中的任务可恢复并继续执行
- 不是从 0 重新开始

## 7. 解锁限流验证

### 步骤

1. 连续输入错误主密码 5 次
2. 第 6 次继续尝试解锁

### 预期结果

- 接口返回 `429`（Too Many Attempts）
- 页面提示需要等待后再重试

## 8. 可选：数据库快速检查

如本机安装了 `sqlite3`：

```bash
sqlite3 ~/.easy_withdraw/easy_withdraw.db "select key from kv_store;"
sqlite3 ~/.easy_withdraw/easy_withdraw.db "select count(*) from withdraw_history;"
sqlite3 ~/.easy_withdraw/easy_withdraw.db "select id,state,done_count,total_count from schedule_jobs order by created_at desc limit 5;"
```

## 9. 验收记录模板

建议在测试过程中记录：

- 测试日期
- 执行人
- 服务启动方式（HTTP/HTTPS）
- 每个测试项结果（Pass/Fail）
- 失败截图与日志

建议最少输出以下结论：

1. 重启后定时任务连续性：Pass / Fail
2. 锁定后任务继续执行：Pass / Fail
