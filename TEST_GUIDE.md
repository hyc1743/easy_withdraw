# Easy Withdraw 测试指引

本指引用于验证以下目标是否生效：

- 仅本机访问（默认 localhost）
- 定时提现进度可同步
- 自动卖出进度可同步
- 关闭浏览器不影响后端任务执行
- 会话锁定后，已启动任务继续执行
- SQLite 持久化与服务重启恢复
- `unlock` 防暴力破解限流

## 1. 测试前准备

### 1.1 环境要求

- Node.js 18+（建议 20+）
- npm 可访问外网
- 可用的 Gate / Binance 测试账号或可容错的测试参数
- Binance 自动卖出需要 API Key 具备现货交易权限

### 1.2 安装依赖

```bash
npm install
```

### 1.3 启动服务

开发模式：

```bash
npm run dev
```

生产模式：

```bash
npm start
```

默认访问地址：

- 本机：`http://127.0.0.1:4217`

## 2. SQLite 初始化与迁移验证

### 步骤

1. 启动服务后检查目录 `~/.easy_withdraw/`
2. 确认存在 `easy_withdraw.db`
3. 若存在历史 `config.json/history.json`，登录后检查账户、模板、历史是否可见

### 预期结果

- SQLite 数据库自动创建
- 旧 JSON 数据自动迁移并可读取
- `schedule_jobs` 支持 `job_type / payload_json / progress_json`

## 3. 单次提现持久化验证

### 步骤

1. 解锁系统
2. 添加地址簿条目（任意测试地址）
3. 执行一次单次提现（成功/失败均记录）
4. 重启服务后再次查看地址簿与历史页

### 预期结果

- 地址簿数据持久化
- 历史记录持久化

## 4. 定时提现同步与浏览器关闭验证

### 步骤

1. 解锁后启动定时提现（建议：间隔 `15s`，次数 `6`）
2. 观察执行 1 次后，直接关闭浏览器
3. 等待约 1 分钟，再次打开页面并解锁
4. 进入“提现”页查看进度面板
5. 刷新页面（F5）再次观察

### 预期结果

- 任务持续执行，不因浏览器关闭中断
- 页面重新打开后可同步到最新进度
- 刷新页面不重置进度

## 5. Binance 自动卖出预校验

### 步骤

1. 使用具备现货交易权限的 Binance API 账户解锁系统
2. 进入“卖出”页，选择 Binance 账户
3. 选择一个现货交易对，例如 `BTCUSDT`
4. 输入每次卖出数量与间隔秒数
5. 点击“预校验”

### 预期结果

- 页面返回当前基础币余额
- 页面显示最小下单量、步进和可执行数量
- 非法交易对或数量过小时，页面明确提示错误

## 6. Binance 自动卖出执行与最后一轮收尾

### 步骤

1. 选择一个余额略大于单次数量的基础币，例如余额 `0.025 BTC`
2. 在“卖出”页将每次卖出数量设置为 `0.01`
3. 将间隔设置为 `5s`
4. 启动自动卖出任务
5. 观察前两轮按 `0.01` 卖出
6. 观察最后一轮按剩余余额卖出并自动完成

### 预期结果

- 后端按秒间隔持续执行，无需浏览器保持打开
- 最后一轮会卖出剩余余额，而不是因为不足单次数量直接跳过
- 任务面板显示交易对、执行日志和累计卖出数量

## 7. 自动卖出失败即停止

### 步骤

1. 选择一个会触发失败的参数，例如最小下单量以下的数量，或临时撤销交易权限
2. 启动自动卖出任务
3. 观察任务日志与状态

### 预期结果

- 当前轮失败后任务立即停止
- 任务日志显示失败原因
- 不会继续自动重试下一轮

## 8. 锁定后继续执行验证（关键）

### 步骤

1. 启动定时提现或自动卖出任务（建议：间隔 `20s`）
2. 立即点击“锁定”
3. 等待 40~60 秒
4. 重新解锁并查看进度

### 预期结果

- 锁定期间任务继续推进
- 进度与日志显示锁定期间产生的执行记录

## 9. 服务重启恢复验证

### 步骤

1. 启动定时提现或自动卖出任务（建议：间隔 `20s`）
2. 任务执行 1~2 次后，停止服务（`Ctrl+C`）
3. 重新启动服务并解锁
4. 打开“提现”页或“卖出”页查看进度

### 预期结果

- 运行中的任务可恢复并继续执行
- 不是从 0 重新开始

## 10. 定时提现回归验证

### 步骤

1. 重新执行一次原有定时提现流程
2. 检查开始、停止、继续、刷新后的进度同步

### 预期结果

- 原有定时提现能力仍可用
- 任务面板可正确显示提现任务

## 11. 解锁限流验证

### 步骤

1. 连续输入错误主密码 5 次
2. 第 6 次继续尝试解锁

### 预期结果

- 接口返回 `429`（Too Many Attempts）
- 页面提示需要等待后再重试

## 12. 可选：数据库快速检查

如本机安装了 `sqlite3`：

```bash
sqlite3 ~/.easy_withdraw/easy_withdraw.db "select key from kv_store;"
sqlite3 ~/.easy_withdraw/easy_withdraw.db "select count(*) from withdraw_history;"
sqlite3 ~/.easy_withdraw/easy_withdraw.db "select id,job_type,state,done_count,total_count from schedule_jobs order by created_at desc limit 5;"
```

## 13. 验收记录模板

建议在测试过程中记录：

- 测试日期
- 执行人
- 服务启动方式（HTTP/HTTPS）
- 每个测试项结果（Pass/Fail）
- 失败截图与日志

建议最少输出以下结论：

1. 重启后定时任务连续性：Pass / Fail
2. 锁定后任务继续执行：Pass / Fail
3. Binance 自动卖出最后一轮收尾：Pass / Fail
